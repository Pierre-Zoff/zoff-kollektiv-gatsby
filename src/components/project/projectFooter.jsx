import React from "react";

const ProjectFooter = (props) => (
  <div className="bg-gray relative flex items-center justify-center py-10">
    <div className="text-white font-medium text-4xl">{props.title}</div>
  </div>
);
export default ProjectFooter;
